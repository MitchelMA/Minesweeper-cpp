add_library(ansi "")

file(GLOB ANSI_LEVEL_SOURCES *.cpp)
file(GLOB ANSI_LEVEL_HEADERS *.hpp *h)

target_sources(ansi
    PRIVATE
    ${ANSI_LEVEL_SOURCES}
    PUBLIC
    ${ANSI_LEVEL_HEADER})

target_include_directories(ansi
    PUBLIC
    ${CMAKE_CURRENT_LIST_DIR})